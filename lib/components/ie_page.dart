import 'package:flutter/material.dart';
class IePage extends StatelessWidget {
  const IePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}